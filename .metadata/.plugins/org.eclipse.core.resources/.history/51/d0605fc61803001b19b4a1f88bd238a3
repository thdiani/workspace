package variaveis;

public class Primitivos {

	public static void main(String[] args) {
		/*	
		 * Tipos primitivos em ordem hierarquica (ocupacao de memoria do menor para maior)
		 * boolean => tipo logico (true ou false)
		 * char => Armazena UM caracter *o q corresponde na tabela ASCI
		 * byte => menor entre os inteiros
		 * short => numeros inteiros
		 * int => numeros inteiros *padrao
		 * long => maior entre os inteiros
		 * float => maior precisao nas casas decimais (mais baixa q o double) ate umas 4 casas
		 * double => maior precisao nas casas decimais *padrao
		 * 
		 * Cast: conversao entre tipos compativeis, apenas com hierarquias diferentes
		 * Parse: conversao entre tipos distintos
		 */
		
		
	}

}
